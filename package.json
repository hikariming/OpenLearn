{
	"scripts": {
		"dev": "concurrently \"pnpm --filter openlearn dev\" \"pnpm --filter server start:dev\"",
		"test:server": "pnpm --filter server test:manual"
	},
	"devDependencies": {
		"@nestjs/cli": "^11.0.12",
		"concurrently": "^9.2.1"
	}
}